let firstName;
let lastName = 'Alguien soy'

// console.log(`${ firstName || 'no tiene valor' } - ${ lastName || 'No tiene valor' }`)

const isActive = true

const message = ( isActive ) ? 'Activo' : 'Inactivo'
console.log(message)
